가공된 raw data의 결측치를 보여주는 python code.
missing-no.ipynb의 파일을 변경하면 다른 파일들도 볼 수 있다.
본격적인 데이터 분석 이전에 결측치를 어떻게 처리해야하는가 다룰려고 만든 코드.

data에 파일을 넣고 분석하면 result-files에 결과가 저장된다.
저장된 결과는 공통적으로 관측된 결측치를 제외한 나머지의 결측치 비율을 표시한다.
그래프도 저장한다.